{
  "name": "@openmini/host",
  "private": true,
  "version": "0.1.0",
  "description": "OpenMini host application — the shell that will load and run mini-apps.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build:fixtures": "node scripts/build-bridge-demo-fixture.mjs",
    "dev": "pnpm run build:fixtures && vite",
    "build": "pnpm run build:fixtures && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "pnpm run build:fixtures && vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@openmini/runtime": "workspace:*",
    "@openmini/sdk": "workspace:*",
    "@openmini/shared": "workspace:*",
    "@openmini/ui": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5"
  }
}
