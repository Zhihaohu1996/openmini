{
  "name": "@openmini/manifest",
  "version": "0.1.0",
  "description": "Parser and validator for the OpenMini Mini App manifest (openmini.json).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src", "dist", "schema"],
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
